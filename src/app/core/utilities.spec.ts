import { Utilities } from './utilities';

describe('Utilities', () => {
  let service: Utilities;

  beforeEach(() => {
    service = Utilities;
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
